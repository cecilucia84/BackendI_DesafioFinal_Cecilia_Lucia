<div class="container">
    {{#each product}}
    <div class="product">
        <h2>{{title}}</h2>
        <img src="{{thumbnail}}" alt="imagen de {{title}}">
        <div class="product__info">
            <p>{{description}}</p>
            {{#if isLoggedIn}}
            <p>Precio: {{price}}</p>
            {{/if}}
            <p>Stock: {{stock}}</p>
            <p>Código: {{code}}</p>
            <p>ID: {{id}}</p>
            <form action="/api/carts/66fdb280ce811c8293e42e41/product/{{id}}" id="formProductToCart" method="post">
                <input id="addToCart" type="submit" value="Agregar al Carrito">
            </form>
        </div>
        <a href="/products">Volver</a>
    </div>
    {{/each}}
</div>